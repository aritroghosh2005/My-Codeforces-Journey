import sys
input = lambda : sys.stdin.readline().rstrip()
 
def solve():
    n = int(input())
    s = input()
 
    lines = list(map(len, s.split('*') ) )
    longestLine = max(lines)
    time = -(-longestLine//2)
    print(time)
 
t = int(input())
for _ in range(t): solve()