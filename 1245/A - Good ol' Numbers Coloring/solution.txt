import sys
input = lambda : sys.stdin.readline().rstrip()
 
from math import gcd
 
def solve():
    a, b = map(int, input().split())
    g = gcd(a,b)
    print("Finite" if g == 1 else "Infinite")
    
t = int(input())
for _ in range(t): solve()